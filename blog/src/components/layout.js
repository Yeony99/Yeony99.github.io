import React from 'react'

export default ({children}) => (
  <div style={{
    // margin: `1rem auto`,
    // padding: `0px 2rem`,
    display: `flex`,
    // position: `relative`,
    flexDirection: `column`}}>
    {children}
  </div>
)
